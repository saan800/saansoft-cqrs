namespace SaanSoft.Cqrs.Middleware;

// public interface ISubscriberMiddleware<TMessage> where TMessage : IMessage
// {
//     Task InvokeAsync(TMessage message, SubscriberHandlerDelegate<TMessage> next, CancellationToken ct);
// }
